---
import BaseLayout from '../layouts/BaseLayout.astro';
import raw from '../../reference-cidr.html?raw';
import { wrapLegacy } from '../lib/wrap.js';
const { title, description, ogTitle, headSchemas, slotHtml } = wrapLegacy(raw);
---
<BaseLayout title={title} description={description} ogTitle={ogTitle} canonicalPath="/reference-cidr.html" page="reference-cidr" navPage="downloads">
  <Fragment slot="head" set:html={headSchemas} />
  <Fragment set:html={slotHtml} />
</BaseLayout>
